spring:
  application:
    name: inventory-service
server:
  servlet:
    context-path: /${spring.application.name}
  port: 9096
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
