spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5431/gateway-service
    username: myuser
    password: secret

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /admin
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
    prometheus:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true
